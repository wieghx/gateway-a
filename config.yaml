# Gateway-a Configuration

server:
  host: 0.0.0.0
  port: 8080

logging:
  level: info

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

database:
  host: localhost
  port: 5432
  user: postgres
  password: ""
  dbname: gateway_a
  sslmode: disable
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 5m
